// https://atcoder.jp/contests/m-solutions2020/tasks/m_solutions2020_d

pub fn run(n: usize, a: Vec<usize>) -> usize {
    let mut dp = vec![1000];

    100
}

/*
for i in 1..n {
        if a[i] > a[i - 1] {
            dp[i] = dp[i - 1] / a[i - 1] * a[i] + dp[i - 1] % a[i - 1];
        } else {
            dp[i] = dp[i - 1];
        }
    }
*/
